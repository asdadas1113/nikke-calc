name: tmp-asuka-annihilation

on:
  push:
    branches:
      - fast-engine-phase2-20260901
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    if: ${{ !contains(github.event.head_commit.message || '', '[skip-asuka-annihilation]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Probe Asuka WILLE annihilation blocker
        run: python -m fast_engine.research.tmp_nayuta_periodic_trace
