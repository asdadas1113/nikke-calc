name: Temporary optimizer refinement benchmark

on:
  pull_request:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        variant: [minimal-3, balanced-6]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Run real one-swap refinement benchmark
        run: PYTHONPATH=. python tests/benchmark_optimizer_refinement_real.py --reference-variant '${{ matrix.variant }}'
